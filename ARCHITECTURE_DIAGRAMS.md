# Architecture Diagrams - Barasiah Integration

## 1. Authentication & Platform Detection Flow

```
┌─────────────────────────────────────────────────────────┐
│           Mobile App / Web Browser                      │
└────────────────┬──────────────────────────────────────┘
                 │
                 │ POST /auth/login
                 │ Email + Password
                 │ [X-Platform: mobile]
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│              Backend API (Node.js)                      │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 1. detectPlatform Middleware                    │  │
│  │    - Check X-Platform header                    │  │
│  │    - Check Origin header (web vs mobile)        │  │
│  │    - Set req.platform = 'web' | 'mobile'       │  │
│  └─────────────────────────────────────────────────┘  │
│                      │                                  │
│                      ▼                                  │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 2. Auth Controller                              │  │
│  │    - Verify email & password                    │  │
│  │    - Generate JWT with:                         │  │
│  │      - user: {id, email, role}                  │  │
│  │      - platform: req.platform                   │  │
│  │      - iat, exp                                 │  │
│  └─────────────────────────────────────────────────┘  │
│                      │                                  │
└──────────────────────┼────────────────────────────────┘
                       │
                       │ Return { accessToken, user }
                       │ JWT = eyJhbGc...
                       ▼
┌─────────────────────────────────────────────────────────┐
│           Mobile App / Web Browser                      │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 3. Decode JWT & Detect Role                     │  │
│  │    - JWT has { user: {id, role}, platform }    │  │
│  │    - Role = 'USER' | 'DELIVERER'               │  │
│  │    - Route based on role:                       │  │
│  │      - USER → MainScreen (customer)            │  │
│  │      - DELIVERER → DelivererMainScreen         │  │
│  └─────────────────────────────────────────────────┘  │
│                      │                                  │
└──────────────────────┼────────────────────────────────┘
```

---

## 2. Role-Based Routing (Frontend)

```
                    ┌─── AuthGate ───┐
                    │ Check JWT Token │
                    └────────┬────────┘
                             │
                  ┌──────────┴──────────┐
                  │                     │
           No Token / Expired    Token Found & Valid
                  │                     │
                  ▼                     ▼
            ┌──────────────┐    ┌────────────────────┐
            │ WelcomeScreen│    │ Decode JWT.user    │
            │ (Login/Signup)│    │ Check role field   │
            └──────────────┘    └────────┬───────────┘
                                         │
                        ┌────────────────┼────────────────┐
                        │                │                │
                   role = "USER"   role = "DELIVERER"  Other
                        │                │                │
                        ▼                ▼                ▼
                 ┌──────────────┐ ┌──────────────────┐ Error
                 │ MainScreen   │ │ DelivererMain    │ Page
                 │              │ │ Screen           │
                 ├──────────────┤ ├──────────────────┤
                 │ • Explore    │ │ • Dashboard ✨   │
                 │ • Orders     │ │ • Pekerjaan      │
                 │ • Chat       │ │ • Aktif          │
                 │ • Profile    │ │ • Chat           │
                 │              │ │ • Profile        │
                 └──────────────┘ └──────────────────┘
```

---

## 3. Deliverer Order Management Flow

```
┌─────────────────────────────────────────────────────────┐
│             DelivererMainScreen (Dashboard)             │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │ Dashboard Widget (TAB 0)                        │  │
│  │ ✓ Stats: New Orders, Active, Completed, Rating │  │
│  │ ✓ Quick Actions: View Orders, View Tasks       │  │
│  │ ✓ API Call: GET /deliverer/dashboard/stats    │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │ Pekerjaan (TAB 1)                               │  │
│  │ ✓ Available Orders List                         │  │
│  │ ✓ API Call: GET /orders/available              │  │
│  │ ✓ Button: "Terima Pesanan"                     │  │
│  └─────────────────────────────────────────────────┘  │
│                      │                                  │
│                      │ User taps "Terima"              │
│                      ▼                                  │
│  ┌─────────────────────────────────────────────────┐  │
│  │ Show Confirmation Dialog                        │  │
│  │ User confirms order acceptance                  │  │
│  └─────────────────────────────────────────────────┘  │
│                      │                                  │
└──────────────────────┼────────────────────────────────┘
                       │
                       │ POST /orders/:id/accept
                       │ + Bearer token
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│              Backend Order Controller                   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 1. Verify deliverer authorized (role check)    │  │
│  │ 2. Find order by ID                            │  │
│  │ 3. Update:                                      │  │
│  │    - status = 'ACCEPTED_BY_DELIVERER'          │  │
│  │    - deliverer_id = req.user.id                │  │
│  │    - accepted_at = NOW()                       │  │
│  │ 4. Return updated order                        │  │
│  └─────────────────────────────────────────────────┘  │
│                      │                                  │
└──────────────────────┼────────────────────────────────┘
                       │
                       │ Return { status: 'sukses', data }
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│             Frontend - Show Success                     │
│                                                         │
│  ✓ Update UI with success message                      │
│  ✓ Navigate to "Aktif" tab (TAB 2)                    │
│  ✓ Show order in ActiveOrdersScreen                    │
│  ✓ Refresh dashboard stats                            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 4. API Endpoint Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    Backend API Routes                      │
└────────────────────────────────────────────────────────────┘

Authentication Routes
├── POST   /api/auth/register           [Public]
├── POST   /api/auth/login              [Public] → Platform detection
└── GET    /api/profile                 [Protected]

Customer Routes
├── POST   /api/orders                  [AUTH + CUSTOMER]
├── GET    /api/orders/my-history       [AUTH + CUSTOMER]
├── GET    /api/orders/:id/offers       [AUTH + CUSTOMER]
└── POST   /api/orders/:id/update-status [AUTH]

Deliverer Routes (NEW)
├── GET    /api/orders/available        [AUTH + DELIVERER] ⭐
├── POST   /api/orders/:id/accept       [AUTH + DELIVERER] ⭐
├── POST   /api/orders/:id/reject       [AUTH + DELIVERER] ⭐
├── GET    /api/orders/deliverer/active [AUTH + DELIVERER] ⭐
├── GET    /api/orders/deliverer/completed [AUTH + DELIVERER] ⭐
└── GET    /api/orders/deliverer/dashboard/stats [AUTH + DELIVERER] ⭐

Legend:
[Public]       = No authentication required
[AUTH]         = Requires valid JWT
[AUTH + ROLE]  = Requires JWT + specific role
⭐            = New endpoints from Barasiah
```

---

## 5. JWT Token Structure

```
┌─────────────────────────────────────────────────────────┐
│              JWT Token Payload (Decoded)                │
└─────────────────────────────────────────────────────────┘

{
  "user": {
    "id": 123,                    ← User ID from database
    "email": "user@example.com",  ← Email (NEW)
    "role": "DELIVERER"           ← Role: USER or DELIVERER
  },
  "platform": "mobile",           ← Platform: web or mobile (NEW)
  "iat": 1705667200,              ← Issued at (timestamp)
  "exp": 1705753600               ← Expires at (timestamp)
}

Token Structure:
┌──────────┬──────────┬───────────┐
│ Header   │ Payload  │ Signature │
├──────────┼──────────┼───────────┤
│ {alg...} │ {user..} │ HMAC-SHA  │
└──────────┴──────────┴───────────┘

Example Token (abbreviated):
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyIjp7ImlkIjoxMjN9...
```

---

## 6. Middleware Chain

```
┌───────────────────────────────────────────────┐
│        Incoming Request                       │
└──────────────┬────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────┐
    │ 1. express.json()            │ ← Parse JSON body
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────┐
    │ 2. detectPlatform Middleware         │ ← (NEW)
    │    Sets req.platform                 │
    └──────────────┬──────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────┐
    │ 3. Route Handler                     │
    │                                      │
    │   Some routes require:               │
    │   - authMiddleware.verifyToken()     │
    │   - authorize('ROLE')                │
    └──────────────┬──────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────┐
    │ 4. Controller Method                 │
    │    - Process business logic          │
    │    - Query database                  │
    │    - Return JSON response            │
    └──────────────┬──────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────┐
    │ Response to Client                   │
    │ { status, data, error }              │
    └──────────────────────────────────────┘
```

---

## 7. Database Flow (Orders)

```
┌─────────────────────────────────────────────┐
│        Orders Table Schema                  │
├─────────────────────────────────────────────┤
│ id: Primary Key                             │
│ item_id: Foreign Key                        │
│ quantity: Integer                           │
│ user_id: Customer (Foreign Key)             │
│ deliverer_id: Deliverer (Foreign Key) [NEW]│
│ status: WAITING_FOR_OFFERS → ACCEPTED... → │
│         COMPLETED [UPDATED]                 │
│ destination: String                         │
│ accepted_at: DateTime [NEW]                 │
│ rejected_by_deliverer: Boolean [NEW]        │
│ rejection_reason: String [NEW]              │
│ created_at: DateTime                        │
│ updated_at: DateTime                        │
└─────────────────────────────────────────────┘

Status Flow:
┌─────────────────┐
│WAITING_FOR_OFFERS│ ← Initial state
└────────┬────────┘
         │
    ┌────▼──────────────────┐
    │ Deliverer receives it  │
    └────┬────────────────┬──┘
         │ ACCEPT         │ REJECT
         ▼                ▼
┌──────────────────────────┐
│ACCEPTED_BY_DELIVERER  WAITING_FOR_OFFERS (loop)
│(deliverer_id assigned)│
└────┬────────┬──────────┘
     │        │
     │ ONGOING
     │        │
     ▼        ▼
 IN_PROGRESS
     │
     │ ON_WAY
     │
     ▼
 COMPLETED ← End state
```

---

## 8. File Structure Summary

```
Root/
├── backend/
│   └── src/
│       ├── middleware/
│       │   ├── authMiddleware.js (UPDATED)
│       │   ├── platformMiddleware.js (NEW)      🟢
│       │   └── roleMiddleware.js (NEW)          🟢
│       ├── routes/
│       │   └── orders.js (UPDATED)
│       ├── controllers/
│       │   ├── authController.js (UPDATED)
│       │   └── orderController.js (UPDATED)
│       └── index.js (UPDATED)
│
├── frontend/
│   └── lib/
│       └── features/
│           └── deliverer/
│               ├── services/
│               │   └── deliverer_service.dart (NEW)     🟢
│               └── screens/
│                   ├── deliverer_dashboard_screen.dart (NEW) 🟢
│                   └── deliverer_main_screen.dart (UPDATED)
│
└── Documentation/
    ├── MIGRATION_PLAN.md (NEW)                  🟢
    ├── BARASIAH_INTEGRATION_SUMMARY.md (NEW)    🟢
    ├── QUICK_START_GUIDE.md (NEW)               🟢
    ├── CHANGELOG.md (NEW)                       🟢
    └── API_TEST_EXAMPLES.http (NEW)             🟢

Legend: 🟢 = Created in this integration
```

---

## 9. Security Model

```
┌──────────────────────────────────────────────────┐
│         Security Layers                          │
└──────────────────────────────────────────────────┘

Layer 1: Platform Detection
├─ Detects web vs mobile
├─ Sets req.platform
└─ Enables platform-specific logic

Layer 2: Authentication
├─ Verify JWT token
├─ Decode user info
└─ Check token expiry

Layer 3: Authorization (Role-Based)
├─ Check user role
├─ Verify route permissions
└─ Return 403 if unauthorized

Layer 4: Business Logic Validation
├─ Order ownership checks
├─ Status transition validation
└─ User permission checks

Layer 5: Response Sanitization
├─ Remove sensitive data
├─ Only return needed fields
└─ Error message generalization
```

---

## 10. Comparison: Before vs After

```
BEFORE Integration
┌──────────────────────────┐
│ Authentication           │
├──────────────────────────┤
│ Basic JWT (no platform)  │
│ No role distinction       │
└──────────────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Routes                   │
├──────────────────────────┤
│ Customer endpoints only   │
│ No deliverer features    │
└──────────────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Frontend                 │
├──────────────────────────┤
│ MainScreen only          │
│ Basic order management   │
└──────────────────────────┘

───────────────────────────────────────

AFTER Integration (UPDATED)
┌──────────────────────────┐
│ Authentication           │
├──────────────────────────┤
│ ✅ Platform-aware JWT    │
│ ✅ Role in token         │
│ ✅ Platform detection    │
└──────────────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Routes                   │
├──────────────────────────┤
│ ✅ Customer endpoints    │
│ ✅ Deliverer endpoints   │
│ ✅ Dashboard stats       │
│ ✅ Order accept/reject   │
└──────────────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Frontend                 │
├──────────────────────────┤
│ ✅ MainScreen (CUSTOMER) │
│ ✅ DelivererMainScreen   │
│ ✅ Dashboard             │
│ ✅ Order management      │
│ ✅ Stats & tracking      │
└──────────────────────────┘
```

---

This visualization helps understand the complete architecture of the integrated system!
